<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>식물친구들</title>
    <link rel="icon" href="/image/logo_small.png" type="image/png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gowun+Batang&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/image/logo_small.png" style="height:40px;"> 식물친구들
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="/plant">My Plant</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/forum">Forum</a>
<!--                </li>-->
<!--                <li class="nav-item">-->
<!--                    <a class="nav-link active" href="/market">Market</a>-->
<!--                </li>-->
                <li class="nav-item">
                    <a class="nav-link active" href="/signUp">SignUp</a>
                </li>

            </ul>
            <form class="d-flex">
                <a class="btn btn-outline-secondary" href="/logout" id="logoutButton">logout</a>
                <a class="btn btn-outline-primary" href="/login" id="loginButton">login</a>
            </form>
        </div>
    </div>
</nav>
<script>
    async function checkLoginStatus() {
        try {
            const response = await fetch('/currentUser');
            if (response.ok) {
                const username = await response.text();
                return username.trim() !== 'anonymousUser';
            } else {
                return false;
            }
        } catch (error) {
            console.error('Error:', error);
            return false;
        }
    }

    async function toggleButton() {
        var loginButton = document.getElementById('loginButton');
        var logoutButton = document.getElementById('logoutButton');

        if (await checkLoginStatus()) {
            loginButton.style.display = 'none';
            logoutButton.style.display = 'block';
        } else {
            loginButton.style.display = 'block';
            logoutButton.style.display = 'none';
        }
    }

    toggleButton();  // 페이지 로드시 버튼 표시 상태를 업데이트합니다.
</script>

<div class="container m-5">
